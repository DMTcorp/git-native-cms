# End-to-end fixtures

Sanitized GitHub API responses are stored in `fixtures/github`; deterministic content fixtures are
stored in `fixtures/content`. Tests must never record access tokens, installation credentials,
private keys, webhook secrets or raw customer content.
