# Astro static example

This build-time delivery example intentionally omits editor, API, OAuth and preview routes. It
demonstrates the supported static matrix: fetch or read an immutable release during the build,
render registered sections, and deploy plain HTML. Use `output: "server"` and the full Astro
integration when visual editing is required.
