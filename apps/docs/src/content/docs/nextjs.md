---
title: Next.js
---

Mount `CmsEditorPage`, the preview renderer and `createNextCmsRouteHandlers` in App Router catch-all
routes. Keep the editor stylesheet inside the `/cms` layout so the public application receives no
CMS client runtime.
