---
title: Content modeling
---

Fields and content types are explicitly registered with `@git-native-cms/schema`. The schema
compiler produces JSON Schema, editor manifests, TypeScript declarations, validation rules and
MCP descriptions without inspecting arbitrary components.
